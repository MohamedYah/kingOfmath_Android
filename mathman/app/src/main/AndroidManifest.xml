<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.mathman">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegistreActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.ListquizActivity"
            android:label="" />
        <activity
            android:name=".activity.QuizGameActivity"
            android:label="" />
        <activity
            android:name=".activity.Tutorial_Activity"
            android:label="" />
        <activity android:name=".activity.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AdventureActivity" />
        <activity android:name=".activity.AdventureGameActivity" />
        <activity android:name=".activity.MultiplayerLocalActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".activity.ListeMatchLeagueActivity"
            android:label="@string/title_activity_liste_match_league"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.TournoiGameActivity" />
        <activity android:name=".activity.LeaderBoardActivity" />
        <activity android:name=".activity.StatisticActivity" />
        <activity android:name=".activity.TournoiGroupActivity" />
        <activity android:name=".activity.ResultatLeagueGameActivity"></activity>
    </application>

</manifest>